apply plugin: "war"

bootWar {
    mainClass = "de.unistuttgart.iste.gits.ExampleserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
